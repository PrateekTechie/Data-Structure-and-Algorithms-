public class pratice {
    public static List<List<Integer>> ThreeSum(int[] nums) {
  List<List<Integer>> result = new ArrayList<>();}
  // if array is null or has less than 3 elements, return empty List 
     if(nums == null || num.length < 3) {
        return result;
    array.sort(nums);
  // so there is the 

    for(int i =0;i<ums.length-2;i++)  {
        int left = i+1;
        int sum =
    }

     }

}
